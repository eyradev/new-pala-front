mutation createUserAddress(
  $addresL1: String!
  $cityId: ID!
  $description: String
) {
  createUserAddress(
    addressL1: $addresL1
    cityId: $cityId
    description: $description
  ) {
    id
  }
}
